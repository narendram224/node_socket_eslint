{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "node.js: debug server",
            "type": "node-terminal",
            "request": "launch",
            "command": "yarn run dev"
        }
    ]
}
